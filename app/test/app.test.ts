import { createApp } from "../app";

// Mock fs for json file saving
// Opening file
// Saving to file
function mockFs(initialJson: Object, expectedSave: Array<Object>) {}

beforeEach(() => {
  jest.clearAllMocks();
});
